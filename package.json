{
  "name": "owlonweb",
  "version": "1.0.0",
  "description": "Another UI framework",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "test",
    "lib": "src",
    "out": "out"
  },
  "scripts": {
    "compile": "tsc -t ES5 src/*.ts",
    "build": "npm run compile && cat src/*.js > index.js",
    "test": "npm run build && mocha -r ts-node/register tests/unittests/**/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "ssh://petr@guitarian.net/~/git/OwlOnWeb"
  },
  "keywords": [
    "ui"
  ],
  "author": "Petr Jindra",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.0.4",
    "@types/mocha": "^2.2.43",
    "ts-node": "^3.3.0",
    "typings": "^2.1.1"
  }
}
